# -*- mode: snippet -*-
# name: Nix DevShell Flake
# key: flake
# --
{
  description = "${1:Project description}";

  inputs = {
    nixpkgs.url = "github:nixos/nixpkgs/nixos-unstable";
  };

  outputs = { nixpkgs, ... }:
    let
      system = "x86_64-linux";
      pkgs = nixpkgs.legacyPackages.\${system};
    in {
      devShells.\${system}.default = pkgs.mkShell {
        buildInputs = with pkgs; [
          $0
        ];

        shellHook = ''

        '';
      };
    };
}